#-------------------------#
#                         #
#         AarikOS         #
#                         #
#-------------------------#
# Copyright (c) 2023, Aarik Pokras. All Rights Reserved.
# Licensed under the MIT License.

insmod read

echo -e "Welcome to AarikOS!\n\n"
while [ 0 ]; do
  echo -n ">> "
  read -ra pro
  if [ -z "$pro" ]; then
    echo ""
  elif [ "$pro" = "help" ]; then
    echo -e "\nAarikOS Commands List:\n"
    echo "halt               Halt the system."
    echo "reboot             Reboot the system."
    echo "clear              Clear the screen."
    echo "help               Display this help message."
    echo "br                 Instructions on reporting a bug."
    echo ""
  elif [ "${pro[1]}" = "clear" ]; then
    clear
  elif [ "${pro[1]}" = "halt" ]; then
    halt
  elif [ "${pro[1]}" = "reboot" ]; then
    reboot
  elif [ "${pro[1]}" = "ls" ]; then
    echo -e "\nDIR: "
    read direc
    echo ""
    ls ${direc}
  elif [ "${pro[1]}" = "br" ]; then
    echo -e "To report a bug, you can go to the GitHub issues page or contact aarik@searchme.one."
  else
    echo -e "\nAShell: ${pro[1]}: command not found."
  fi
done

